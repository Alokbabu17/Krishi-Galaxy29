#include <iostream>
using namespace std;

int main()
{
    
    int g;
    int j;
    cout << "Enter the value of g : " << endl;
    cin >> g;
    
    cout << "enter the value of j: " << endl;
    cin >> j;
    
    cout << "sum of the g and j are: " << g + j << endl;
    cout << "sum of the g and j are: " << g + j << endl;
    cout << "sum of the g and j are: " << g + j << endl;
    cout << "sum of the g and j are: " << g + j << endl;
    cout << "sum of the g and j are: " << g + j << endl;
    cout << "sum of the g and j are: " << g + j << endl;
    cout << "sum of the g and j are: " << g + j << endl;
    cout << "sum of the g and j are: " << g + j << endl;
    cout << "sum of the g and j are: " << g + j << endl;
}